import Register from './Register';

describe('Register component', () => {

    test('should write tests', () => {
        // Tests have not been implemented here due to time constraints.
        expect(true).toEqual(true);
    });
})